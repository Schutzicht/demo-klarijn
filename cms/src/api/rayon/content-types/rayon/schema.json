{
  "kind": "collectionType",
  "collectionName": "rayons",
  "info": {
    "singularName": "rayon",
    "pluralName": "rayons",
    "displayName": "Rayons",
    "description": "Geografische rayons waar Klarijn actief is of komt. Verschijnt op de Franchise-pagina."
  },
  "options": { "draftAndPublish": true },
  "pluginOptions": {},
  "attributes": {
    "city": {
      "type": "string",
      "required": true,
      "description": "Plaatsnaam van het rayon. Voorbeeld: 'Zwolle', 'Rotterdam'."
    },
    "status": {
      "type": "enumeration",
      "enum": ["actief", "pilot", "beschikbaar", "voorbereiding"],
      "default": "beschikbaar",
      "description": "Pilot = eerste rayon waar het wordt uitgetest. Actief = rayonhouder is bezig. Beschikbaar = zoekt nog rayonhouder. Voorbereiding = wordt later opgestart."
    },
    "displayOrder": {
      "type": "integer",
      "default": 0,
      "description": "Volgorde in de lijst. Lager getal = bovenaan."
    }
  }
}

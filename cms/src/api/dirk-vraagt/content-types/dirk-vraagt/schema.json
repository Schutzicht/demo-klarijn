{
  "kind": "collectionType",
  "collectionName": "dirk_vraagts",
  "info": {
    "singularName": "dirk-vraagt",
    "pluralName": "dirk-vraagts",
    "displayName": "Vraag het Klarijn (Q&A)",
    "description": "Vraag-en-antwoord paren voor de Vraag-het-Klarijn sectie op de site."
  },
  "options": { "draftAndPublish": true },
  "pluginOptions": {},
  "attributes": {
    "who": {
      "type": "string",
      "required": true,
      "description": "Voornaam van degene die de vraag stelt. Voorbeeld: 'Dirk', 'Mariska'."
    },
    "role": {
      "type": "string",
      "description": "Beroep + plaats. Voorbeeld: 'Aannemer · Apeldoorn'."
    },
    "photo": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "photoUrl": {
      "type": "string",
      "description": "Fallback-pad als nog geen foto is geupload."
    },
    "question": {
      "type": "text",
      "required": true,
      "description": "De vraag zoals de bezoeker hem ziet."
    },
    "answer": {
      "type": "text",
      "required": true,
      "description": "Klarijn's antwoord. Helder, geen jargon."
    },
    "variant": {
      "type": "enumeration",
      "enum": ["klant", "franchise"],
      "default": "klant",
      "required": true,
      "description": "Klant = voor de gewone bezoeker. Franchise = voor juristen die overwegen mee te doen."
    },
    "displayOrder": {
      "type": "integer",
      "default": 0,
      "description": "Lager getal = eerder in de lijst."
    }
  }
}

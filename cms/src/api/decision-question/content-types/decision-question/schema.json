{
  "kind": "collectionType",
  "collectionName": "decision_questions",
  "info": {
    "singularName": "decision-question",
    "pluralName": "decision-questions",
    "displayName": "Match-test vraag",
    "description": "Vragen voor de franchise match-test op /franchise/beslisboom."
  },
  "options": { "draftAndPublish": true },
  "pluginOptions": {},
  "attributes": {
    "question": {
      "type": "text",
      "required": true,
      "description": "De vraag aan de jurist. Voorbeeld: 'Heb je minimaal 3 jaar werkervaring als jurist?'"
    },
    "answerOptions": {
      "type": "component",
      "repeatable": true,
      "component": "tree.answer-option",
      "required": true,
      "min": 2,
      "max": 5,
      "description": "De keuzes (2 tot 5). Klik 'Add an entry' om er een toe te voegen. Score bepaalt de match."
    },
    "displayOrder": {
      "type": "integer",
      "default": 0,
      "description": "Lager getal = eerder in de match-test."
    }
  }
}
